function randomPoint(a, b, n){    var randArray = [];    // given A, B, dimensions of rectangle  	// given g, granularity of shorter side    var am, bm;    g = 1;    if(a > b){    	bm = g;    	am = Math.floor(g*a/b);    } else {    	am = g;    	bm = Math.floor(g*b/a);    }    a -= 20;    b -= 30;    for (var i = 0; i < n; i++) {    	var av = a * Math.random() + 10;    	var bv = b * Math.random() + 10;    	randArray.push({x: av, y: bv});    }    return randArray;}function getRandomInt(min, max) {  min = Math.ceil(min);  max = Math.floor(max);  return Math.floor(Math.random() * (max - min)) + min;}function getRandomIntInclusive(min, max) {  min = Math.ceil(min);  max = Math.floor(max);  return Math.floor(Math.random() * (max - min + 1)) + min;}